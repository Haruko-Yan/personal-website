<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
        <!--Import Google Icon Font-->
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <link rel="stylesheet" href="/styles/materialize.min.css">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css">
        <link rel="stylesheet" href="/styles/user.css">
    </head>
    <body>
        <main>
            <div class="container" style="padding-left: 0;">
                <form action="http://image.ziqingyan.tech/manage/editList/edit/<%=article._id %>" enctype="multipart/form-data" method="post">
                    <div class="mb-3">
                        <label for="author" class="form-label">Author</label>
                        <input type="text" class="form-control author" id="author" value="<%=article.author %>" name="author">
                    </div>
                    <div class="mb-3">
                        <label for="title" class="form-label">Title</label>
                        <input type="text" class="form-control title" id="title" value="<%=article.title %>" name="title">
                    </div>
                    <div class="mb-3">
                        <label for="tag" class="form-label">Tags (Separate by space)</label>
                        <input type="text" class="form-control tag" id="tag" value="<%=article.tag.toString().replaceAll(",", " ") %>" name="tag">
                    </div>
                    <div class="mb-3">
                        <label for="text" class="form-label">Text</label>
                        <textarea class="form-control text text" id="text" name="text" rows="3"><%=article.text %></textarea>
                    </div>
                    <div class="mb-3">
                        <label for="uploadedImages" class="form-label">Upload your images</label>
                        <input class="form-control uploadedImages" type="file" id="uploadedImages" name="images" accept="image/*" multiple>
                    </div>
                    <input class="submit btn btn-primary multi-submit" type="submit">
                </form>
            </div>
        </main>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
        <!-- Compiled and minified JavaScript -->
        <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
        <script>
            $(window).on('beforeunload', function(){
                return 'Are you sure you want to leave?';
            });
            $('.submit').on('click', function () {
                $(window).off("beforeunload");
            });
        </script>
        <script src="/js/person.js"></script>
    </body>
</html>